backend = "glx"

animations = (
  {
    triggers = ["open", "show"];
    preset = "appear";
    duration = 0.25;
    scale = 0;
  },
  {
    triggers = ["close", "hide"];
    preset = "disappear";
    duration = 0.25;
    scale = 0;
  },
  {
    triggers = ["geometry"];
    preset = "geometry-change";
  }
)
