return {
	volume_slider = require("ui.controls.volume_slider"),
	brightness_slider = require("ui.controls.brightness_slider"),
	wifi = require("ui.controls.wifi"),
	bluetooth = require("ui.controls.bluetooth"),
	volume = require("ui.controls.volume"),
}
