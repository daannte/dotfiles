return {
	clock = require("ui.bar.modules.clock"),
	taglist = require("ui.bar.modules.taglist"),
	battery = require("ui.bar.modules.battery"),
	dash = require("ui.bar.modules.dash"),
	wifi = require("ui.bar.modules.wifi"),
	bluetooth = require("ui.bar.modules.bluetooth"),
}
